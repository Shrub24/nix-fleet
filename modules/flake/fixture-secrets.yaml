# Placeholder SOPS file for the fixture evaluation class: never decrypted,
# never activated. Its only job is to be a path that exists and parses as YAML,
# which is all the aspects' existence gates and sops-nix's manifest validation
# ask for. Real secret files stay in consumer repositories.
telegram_bot_token: fixture-token-placeholder
ntfy_token: fixture-token-placeholder
beszel:
  key: fixture-key-placeholder
  token: fixture-token-placeholder
tailscale:
  auth_key: fixture-token-placeholder
signing_key: fixture-key-placeholder
s3_access_key_id: fixture-token-placeholder
s3_secret_access_key: fixture-token-placeholder
niks3:
  api_token: fixture-token-placeholder
